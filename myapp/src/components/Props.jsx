export default function Contact({ name, age }) {
    return (
        <>
            <p>call / uncomment "props" component in "App.jsx" to view props </p>
            <h3>{name} {age}</h3>
        </>
    )
}

